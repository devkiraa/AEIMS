<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>
<body class="min-h-screen bg-gray-100 relative">
    <div class="bg-white h-screen w-full max-w-md absolute right-0 top-0 flex flex-col justify-center p-12 rounded-l-lg shadow-lg">
        <h1 class="text-2xl font-bold mb-6 text-center">Forgot Password?</h1>
        <form action="/forgot-password" method="POST">
            <!-- Email Field -->
            <div class="mb-4">
                <label for="email" class="block text-gray-700">Email ID</label>
                <input type="email" id="email" name="email" placeholder="Email" class="w-full mt-1 p-2 border border-gray-300 rounded-md" required>
            </div>

            <!-- Mobile Field -->
            <div class="mb-6">
                <label for="mobile" class="block text-gray-700">Mobile</label>
                <input type="text" id="mobile" name="mobile" placeholder="+91" class="w-full mt-1 p-2 border border-gray-300 rounded-md" required>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-md text-center font-medium hover:bg-blue-700 transition duration-200">Next</button>
        </form>

        <!-- Bottom Text -->
        <p class="text-center text-sm text-gray-500 mt-6">
            Mistake? <a href="/auth/login" class="text-blue-500 hover:underline">Sign in</a>
        </p>
    </div>
</body>
</html>
